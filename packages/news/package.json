{
  "name": "@daily-news-discord/news",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w --preserveWatchOutput",
    "lint": "eslint",
    "clean": "rimraf .turbo && rimraf node_modules && rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@daily-news-discord/environment": "workspace:*",
    "@daily-news-discord/logger": "workspace:*",
    "axios": "^0.26.1"
  },
  "devDependencies": {
    "@daily-news-discord/config": "workspace:*"
  }
}
