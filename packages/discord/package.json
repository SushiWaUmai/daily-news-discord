{
  "name": "@daily-news-discord/discord",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w --preserveWatchOutput",
    "lint": "eslint",
    "clean": "rimraf .turbo && rimraf node_modules && rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@daily-news-discord/database": "workspace:*",
    "@daily-news-discord/environment": "workspace:*",
    "@daily-news-discord/logger": "workspace:*",
    "@daily-news-discord/news": "workspace:*",
    "discord.js": "^13.6.0"
  },
  "devDependencies": {
    "@daily-news-discord/config": "workspace:*"
  }
}
